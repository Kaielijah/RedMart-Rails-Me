<header class="navbar navbar-collapse navbar-fixed-top navbar">
  <button type="button" class="navbar-toggle dropdown-toggle gone" data-toggle="dropdown" data-target="#myNavbar">
            <span class="glyphicon glyphicon-option-vertical"></span>

          </button>
      <div class="container">
        <%= link_to "RedMart", '/home', id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right navbar-collapse collapse">
            <li><%= link_to "Products", products_path %></li>
            <% if logged_in? %>
              <% if admin? %>
                      <li><%= link_to "Users", users_path %></li>

                      <% end %>
                      <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" data-delay="1000" tabindex="-1">
                          Account <b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu ">
                          <li><%= link_to "Profile", current_user %></li>
                          <li><%= link_to "Settings", edit_user_path(current_user)  %></li>
                          <li class="divider"></li>
                          <li>
                            <%= link_to "Log out", logout_path, method: "delete" %>
                          </li>
                        </ul>
                      </li>
                    <% else %>
                      <li><%= link_to "Log in", login_path %></li>
                    <% end %>

          </ul>
        </nav>
      </div>
    </header>
